# Comma-separated list of allowed frontend origins for CORS.
# Set this in Render's Environment tab to your deployed Vercel URL.
ALLOWED_ORIGINS=https://your-app.vercel.app

# Leave unset/false in production. Render sets PORT automatically.
FLASK_DEBUG=false
PORT=5000
